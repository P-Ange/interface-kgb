{% extends 'base.html.twig' %}

{% block title %}KGB Interface{% endblock %}

{% block body %}
    <div class=" row justify-content-center text-center mt-5">
        <div class"col-auto">
        <h1 class="text-white mb-5">Détails du refuge</h1>
        <style>
            .tftable {font-size:14px;color:#fbfbfb;border-width: 1px;border-color: #686767;border-collapse: collapse;}
            .tftable tr {background-color:#2f2f2f;}
            .tftable td {font-size:14px; border-width: 1px;padding: 8px;border-style: solid;border-color: #686767;}
            .tftable tr:hover {background-color:#171515;}
            .premiereligne {font-size:14px;background-color:#171515;border-width: 1px;padding: 8px;border-style: solid;border-color: #686767;text-align:center;}
        </style>
        <div class="container  d-flex justify-content-center" >
            <div class="table-responsive ">
                <table class="tftable" >
                    <tbody>
                    <tr><td class="premiereligne fw-bold">ID</td><td class="premiereligne">{{ hidingplaces.id }}</td></tr>
                    <tr><td class="fw-bold">TYPE</td><td>{{ hidingplaces.type }}</td></tr>
                    <tr><td class="fw-bold">ADRESSE</td><td>{{ hidingplaces.address}}</td></tr>
                    <tr><td class="fw-bold">NOM DE CODE</td><td>{{ hidingplaces.alias}}</td></tr>
                    <tr><td class="fw-bold">PAYS D'ORIGINE</td><td>{{ hidingplaces.country}}</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    </div>
    <div class="container mt-2 d-flex justify-content-center">
        <a href="{{ path('hidingplaces') }}" class="btn btn-secondary">Retour</a></div>
{% endblock %}
